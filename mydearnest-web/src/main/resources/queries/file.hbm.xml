<?xml version="1.0"?>
<!DOCTYPE hibernate-mapping PUBLIC "-//Hibernate/Hibernate Mapping DTD 3.0//EN" "http://www.hibernate.org/dtd/hibernate-mapping-3.0.dtd">
<hibernate-mapping>
	<query name="findImageSourceByFolder">
		<![CDATA[
		select 
			post.imageSource as imageSource
		from 
			Post as post
			left join post.folders folder
			left join post.imageSource
		where 
			folder.id = :folderId
		and
			post.deletedOn is null
		]]>
	</query>
	
</hibernate-mapping>